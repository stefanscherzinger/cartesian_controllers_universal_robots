<launch>

  <arg name="rvizconfig" default="$(find cartesian_controllers_ur_robots)/etc/setup.rviz" />

  <!-- Visualization -->
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(arg rvizconfig)" required="true" />

</launch>
